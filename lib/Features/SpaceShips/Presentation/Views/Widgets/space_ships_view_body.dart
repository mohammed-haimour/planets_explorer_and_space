import 'package:flutter/material.dart';

class SpaceShipsViewBody extends StatelessWidget {
  const SpaceShipsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
