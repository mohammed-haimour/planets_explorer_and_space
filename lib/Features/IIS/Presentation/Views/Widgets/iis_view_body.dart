import 'package:flutter/material.dart';

class IisViewBody extends StatelessWidget {
  const IisViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
